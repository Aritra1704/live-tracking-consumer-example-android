<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="io.hypertrack.livetrackingconsumer">

    <application
        android:allowBackup="true"
        android:name="io.hypertrack.livetrackingconsumer.LiveTrackingConsumerApplication"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="io.hypertrack.livetrackingconsumer.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name="io.hypertrack.livetrackingconsumer.YourMapActivity"
            android:theme="@style/TrackingActivityTheme"
            android:screenOrientation="portrait"/>

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value=<Enter your google API key> />

    </application>

</manifest>